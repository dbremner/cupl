COMMENT	COMPUTE THE SUM AS K GOES FROM 1 TO N OF K!/X**K
	READ N, X
	LET SUM = 0
	LET M = 1
	LET TERM = 1
	PERFORM ADD N TIMES
	WRITE SUM
	STOP
ADD	BLOCK
	LET SUM = SUM + TERM
	LET TERM = TERM * M/X
	LET M = M + 1
ADD	END

*DATA	N = 8, X = 10
