COMMENT READ A NUMBER AND TEST IT FOR PRIMALITY
	READ M
	PERFORM TEST FOR N=2
	PERFORM TEST FOR N=3 TO SQRT(M) BY 2
TEST	BLOCK
	IF N*FLOOR(M/N) = M THEN GO TO FOUND
TEST	END
	WRITE /M, 'IS A PRIME'
	STOP
FOUND	WRITE /M, 'IS NOT A PRIME'
	STOP
